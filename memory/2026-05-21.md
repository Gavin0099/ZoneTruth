# 2026-05-21

## Summary
- Fixed a bug in `scripts/run-runtime-governance.sh` where empty array expansions of `smoke_overrides` caused "unbound variable" errors under `set -u` on macOS shell environments.
- Disabled `set -u` check globally in the governance script by changing `set -euo pipefail` to `set -eo pipefail` to ensure robust cross-platform git pre-push execution.
- Fixed observation primitive calculation logic in `WorkoutObservationPrimitiveBuilder` to return nil drift/stability and empty distribution when sample count is sparse/insufficient, matching snapshot expectations.
- Localized reason snippets and assertion strings to Chinese across core tests and sample cases to resolve suite-wide failures due to translation discrepancies.
- 填入真實 Strava 憑證（Client ID: 248735）至 `StravaAdapter.swift` 中的 `StravaCredentials` enum；Repo 為 private，憑證由使用者確認後直接導入。
- 修正 Strava OAuth "Invalid redirect URL" 錯誤：將 `redirect_uri` 從 `zonetruth://strava/callback` 改為 `zonetruth://localhost/strava/callback`（host 改為 `localhost`），對應 Strava API 設定頁的 Authorization Callback Domain 欄位應填 `localhost`。
- 將 Strava OAuth 授權流程從 `openURL + .onOpenURL` 改為 `ASWebAuthenticationSession`：在 app 內開啟 mini-browser，自動攔截 `zonetruth://` redirect，不再依賴 Safari 跳轉，修正 iOS 16+ Safari 不自動返回 app 的問題。
- 進一步簡化 redirect_uri 為 `zonetruth://localhost`（移除路徑），符合 StravaKit 等成熟 SDK 的慣例，修正 Strava 伺服器對帶路徑 custom scheme URI 的驗證拒絕問題。
- 修正 `StravaOAuthCoordinator.authenticate` 的 continuation double-resume crash：移除 `session.start()` 失敗分支的 `resume` 呼叫，改為只讓 completion handler 獨占 resume，避免兩路同時觸發的 fatal error。
- 修正無效 SF Symbol 名稱 `"activity"` → `"waveform.path.ecg.rectangle"`。
- 修正 iOS session 檔案無法寫入的根本原因：`DataImport` 中的 `currentDirectoryPath` 在 iOS 裝置上指向 `/`（無寫入權限），改為 `documentDirectory`，確保 strava-session.json 能正確寫入與讀取。
- `connectStrava()` 改為不論 `handle()` 成功或失敗都呼叫 `refreshWorkouts()`，並在 banner 顯示 token 交換結果訊息，方便診斷 Strava 授權後活動未載入的原因。
- 修正 token exchange URL：`/oauth/v3/token` → `/oauth/token`。Strava 的 OAuth 端點不含 `/v3/`，該前綴只用於 REST API 路徑，錯誤的 URL 導致 HTTP 404 → token 交換靜默失敗。
- 改善 Strava 活動載入的診斷訊息：`StravaClientError.networkError` 加入 `statusCode: Int` associated value；`validateHTTP` 帶出 HTTP 狀態碼；`StravaActivityRepository.refreshResult()` 的 catch block 顯示具體錯誤（HTTP 401 / 403 / 其他），方便診斷 token 不足權限或 API 錯誤的問題。
- 加入暫時性 debug print：`FileStravaSessionStore.loadSession/saveSession`、`SystemStravaClient.fetchRecentActivities`、`fetchActivitySummaries` 各加入 `[Strava]` 前綴的 print 輸出，協助從 Xcode console 追蹤活動未顯示的根本原因。
- 確認根本原因：Strava API 成功回傳 20 筆活動（HTTP 200），但 `stravaAsyncMap` 對每筆活動依序發出 streams 心率請求，20 次序列 API 呼叫在手機上耗時過長（可能 10s+），導致 UI 看起來沒有資料。改為使用 `withThrowingTaskGroup` 並行撈取全部活動的心率串流，20 個請求同時發出，大幅縮短等待時間。
- 並行優化後仍無法顯示（fetchRecentActivities: done 20 snapshots 出現後 UI 依然空白）；加入 `[ViewModel] apply:` print 追蹤 apply() 是否被呼叫、source 與 count 是否正確。
- 找到真正根因：`apply: source=healthKit, count=30`。HealthKit 同時也授權了且有 30 筆，CompositeWorkoutRepository 依序取第一個非空結果，Strava 的 20 筆被忽略。
- 最終修正：改為合併所有真實來源（HealthKit + Strava），依 startDate 降序排序，最多 100 筆。加入 `WorkoutDataSource.combined = "Apple Health + Strava"`；`canConnectStrava` 和 `canRequestHealthAccess` 均在 combined 時隱藏對應按鈕；`requestHealthAccess()` 改為觸發 HealthKit auth 後做完整 merged refresh。
- UI 整體優化（一次性）：(1) 移除所有 debug print；(2) WorkoutInput 加 dataSource: String? 欄位，Strava 設 "strava"、HealthKit 設 "healthkit"；(3) WorkoutRowView 顯示來源 badge（橘色 S / 粉紅 HK）；(4) CompositeWorkoutRepository 加去重邏輯（同類型、起始時間差 <5 分鐘視為重複，保留心率樣本較多者）；(5) Strava intent 對應：Run/Ride → zone2、WeightTraining/Crossfit → strength、其他 → activityReview；(6) StravaCallbackHandler 加 disconnect()、StravaSessionStore 加 clearSession()、ViewModel 加 disconnectStrava()、banner 加「中斷 Strava」按鈕；(7) apply() 改為不再強制選第一筆活動，修正 iPhone NavigationSplitView 開啟 app 直接跳到詳情頁的問題。
- 完成 P2f closeout guard 落地：`scripts/closeout_workout_evaluation.sh` 新增 weekly snapshot 驗證、Weekly UI 禁用詞檢查、`WeeklyDashboardView` smoke compile guard；`README.md`/`plan.md` 同步更新。
- 修正 Weekly 觀測語義錯位：`WeeklyObservationBuilder` 改為支援 `asOf`，週內顯示改成「截至目前第 N 天」而非預設整週口吻；同時 `strengthDays` 計算納入 `workoutType == .strengthTraining` 以避免肌力日漏算。
- 更新 weekly 測試與 fixture：`WeeklyObservationTests`/`WeeklyLoadPolicyTests`/`WeeklyLoadSnapshotTests` 調整為 deterministic week-end as-of，並更新 `weekly_load_policy_snapshot.json`。
- 完成 P3a：新增 workout-level HRV observation import。`WorkoutInput` 新增 `hrvSDNNMilliseconds`，HealthKit 授權與查詢加入 `heartRateVariabilitySDNN`，view model 重寫流程保留 HRV 欄位，對應 repository mapping test 通過。
- 建立產品教義規格 `docs/TRAINING_PRODUCT_DOCTRINE.md`（P3b–P3i）：正式定義 action-guiding、state-transition、evidence-typed output、claim authority matrix、uncertainty rendering、non-authority reminder 與 guardrails。
- 推進 P3d/P3f UI 治理實作：Weekly 決策卡加入 authority/evidence chips、低證據降視覺權重、non-authority reminder、adaptation direction 區塊；並新增 data freshness authority（fresh/partial/stale/missing）與 stale/missing 強制降級 authority 規則。
- 新增對應測試守護：`testWeeklyAuthorityRenderingDowngradesUnderLowConfidence`、`testWeeklyAdaptationSignalUsesBoundedDirectionClasses`、`testWeeklyFreshnessSignalClassifiesFreshPartialStaleMissing` 以及 weekly dashboard smoke test 全數通過。

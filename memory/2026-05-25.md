# 2026-05-25

## Session Log
- what changed: pulled latest `ai-governance-framework` (source `../ai-governance-framework-temp` @ `2a8ed76`) and imported governance-facing updates into this repo; recovered from accidental root-level import spill and re-applied safe import set.
- commit hash: d4d6daf
- test evidence: governance pre-push gate executed and reported missing daily memory file; import commit created successfully.
- next step: push updated commits, then assess PLAN header compliance (`最後更新` field) and decide whether to align now.
- what changed: aligned PLAN header fields (`最後更新`/`Owner`/`Freshness`) and added ignore rules for runtime artifacts/candidates/python caches/local IDE state.
- commit hash: a4c4c4e
- test evidence: `git status --short --ignored` shows `artifacts/runtime/*` and `memory/candidates/*` now ignored (`!!`), PLAN header fields present.
- next step: push this memory-backed governance hygiene update and monitor next gate run for remaining advisories only.
- what changed: removed tracked local-noise files from git index (`.DS_Store`, `xcuserdata`, Python `__pycache__`/`.pyc`) via `git rm --cached`; added `**/xcuserdata/` ignore rule.
- commit hash: pending
- test evidence: `git ls-files | rg '(\.DS_Store$|xcuserdata/|__pycache__/|\.pyc$)'` reduced to zero after commit target; files remain locally as untracked/ignored.
- next step: push cleanup commit and keep governance working tree focused on source-of-truth artifacts only.
- what changed: implemented intent-source workflow end-to-end (auto/userOverride), persisted per-workout intent overrides, added workout-type default intent settings, bulk apply with scoped preview, and weekly override insight card.
- commit hash: ef701be
- test evidence: `swift test --filter "(WeeklyObservationTests.testBalancedWeek|ZoneTruthAppTests.testWeeklyDashboardViewSmokeCompiles|ZoneTruthAppTests.testObservationBridgeProducesValidWorkoutObservation)"` passed.
- next step: push these feature commits and verify governance runtime gate remains green with updated memory trail.
- what changed: established epistemic UI governance slice with semantic policy, product claim boundary, app-store copy policy, interaction authority policy, and runtime structural guard enforcement in closeout.
- commit hash: pending
- test evidence: `bash -n scripts/closeout_workout_evaluation.sh` passed; structural guard checks added for forbidden interaction authority patterns and required weekly surface order.
- next step: push governance slice, then define P0-C inference governance (`INFERENCE_POLICY.md` + provenance schema + non-interventional authority ceiling checks).
- what changed: added inference governance layer (`docs/INFERENCE_POLICY.md` + `schemas/inference_provenance.schema.json`) and enforced `inference_authority_guard` in workout closeout to block upstream authority inflation.
- commit hash: pending
- test evidence: `bash -n scripts/closeout_workout_evaluation.sh` passed; guard references and required artifact checks verified via `rg`.
- next step: commit and push this P0-C slice, then continue with runtime provenance integration into actual inference payloads.
- what changed: integrated inference provenance into weekly runtime signals (adaptation/state), rendered provenance section in advanced UI, and added tests locking `non_interventional` authority ceiling with missing-evidence disclosure.
- commit hash: pending
- test evidence: `swift test --filter testWeeklyAdaptationSignalUsesBoundedDirectionClasses`; `swift test --filter testWeeklyInferenceProvenanceRevealsMissingEvidenceUnderSparseCoverage`; `swift test --filter testWeeklyDashboardViewSmokeCompiles` all passed.
- next step: wire provenance into shared core model/output contract so non-UI surfaces (exports/API) carry the same authority ceiling metadata.
- what changed: moved inference provenance contract into ZoneTruthCore (`InferenceProvenance`, `InferenceType`, `InferenceAuthorityCeiling`, `MissingEvidence`, `DerivedFromSignal`, factory + fail-closed validator), refactored weekly UI signals to consume core-generated provenance only, and added core contract guard in closeout.
- commit hash: pending
- test evidence: `swift test --filter testInferenceProvenanceFactoryProducesFailClosedWeeklyContract`; `swift test --filter testBoundedInferenceProvenanceFailsWithoutMissingEvidence`; `swift test --filter testWeeklyInferenceProvenanceRevealsMissingEvidenceUnderSparseCoverage`; `bash -n scripts/closeout_workout_evaluation.sh` all passed.
- next step: extract WeeklyAdaptationSignal/WeeklyTrainingStateSignal inference assembly into ZoneTruthCore so app layer becomes pure render.
- what changed: moved weekly signal assembly into ZoneTruthCore (`WeeklySignals.swift`) including adaptation/state construction, confidence/freshness classification, and provenance contract wiring; app now renders core-provided signals only.
- commit hash: pending
- test evidence: `swift test --filter testWeeklyAdaptationSignalUsesBoundedDirectionClasses`; `swift test --filter testWeeklyTrainingStateSignalCoversStateProgression`; `swift test --filter testWeeklyDashboardViewSmokeCompiles`; `bash -n scripts/closeout_workout_evaluation.sh`; UI-boundary grep guard passed (no forbidden tokens in WeeklyDashboardView).
- next step: add dedicated core-level tests for freshness/confidence classifier edge cases now that assembly lives in core.
- what changed: added `WeeklySignalsCoreTests` to pin freshness/confidence/classifier/authority edge behavior in ZoneTruthCore after signal assembly migration.
- commit hash: pending
- test evidence: `swift test --filter WeeklySignalsCoreTests` and `swift test --filter testWeeklyAdaptationSignalUsesBoundedDirectionClasses` passed.
- next step: gradually de-duplicate overlapping App-side semantic tests that now belong to Core while keeping UI-render smoke coverage in App tests.
- what changed: deduplicated weekly semantic tests from App layer after core migration; removed App tests now covered by `WeeklySignalsCoreTests` (freshness/confidence/inference-classifier/authority mapping), kept App visual/render tests.
- commit hash: pending
- test evidence: `swift test --filter WeeklySignalsCoreTests`; `swift test --filter testLowEvidenceCannotRenderHighAuthorityVisuals`; `swift test --filter testWeeklyDashboardViewSmokeCompiles` passed.
- next step: optionally add a small "coverage ownership" note in docs to prevent future duplicate rule tests drifting back into App layer.
- what changed: added `docs/TEST_RESPONSIBILITY_BOUNDARY.md` to codify Core/App/closeout test ownership and prevent boundary drift.
- commit hash: pending
- test evidence: doc-only governance update; no runtime behavior change.
- next step: reference this boundary doc from future test PR reviews to block duplicated inference-semantics tests in App layer.
- what changed: wired `TEST_RESPONSIBILITY_BOUNDARY` into closeout with fail-close checks (doc presence/content + App-test ban on core inference semantic calls).
- commit hash: pending
- test evidence: `bash -n scripts/closeout_workout_evaluation.sh` and `swift test --filter WeeklySignalsCoreTests` passed.
- next step: if needed, extend boundary guard to additional App test files beyond `ZoneTruthAppTests.swift`.

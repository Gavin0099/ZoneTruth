# 2026-05-22

## Summary
- 推進 P3d closeout guards：將視覺權威不透明度渲染規則（WeeklyAuthorityRendering）綁定決策權威等級（WeeklyDecisionAuthority），避免低證據情境顯示高強調視覺。
- 修正 `WeeklyDashboardView.swift` 中的 `WeeklyOverviewCard`，傳遞計算後的 `authority` 而非原始 policy.confidence，藉以限制不透明度。
- 於 `ZoneTruthAppTests.swift` 中新增並修正測試 `testWeeklyAuthorityRenderingDowngradesUnderLowConfidence`，以及新增 `testLowEvidenceCannotRenderHighAuthorityVisuals` 測試，驗證在低證據（stale/missing/low confidence）下使用最低視覺不透明度。
- 更新 `scripts/closeout_workout_evaluation.sh`，新增 `visual_authority_bypass_detected` 靜態守門機制，防止 UI 渲染程式碼直接調用原始 policy 信心度作為不透明度依據。
- 建立今日 memory 記錄檔以通過 git pre-push 門禁檢查。

## Session 2 (Phase B–C–D + governance sync)
- 從 `ai-governance-framework` 拉取最新版（commit 6bb51ae），同步 `e1b_consumer_audit.py`（加入 P5 跨供應商語意邊界）、`session_end_hook.py`（codeburn phase2 anchors）、`memory_record.py`（deduplication fix）。
- 在 `expansion_boundary_checker.py` 登記 8 個 consumption-visibility transitional keys，消除 `expansion_boundary FAIL`，drift check 恢復全 PASS。
- **Phase B**：`WorkoutInput` 新增 `activeCaloriesKcal`、`totalDistanceMeters` 兩個可選欄位；`HealthKitAdapter` 新增 `activeEnergyBurned`、`distanceWalkingRunning/Cycling/Swimming` 授權申請與讀取。
- **Phase C**：`WorkoutObservationPrimitives` 新增 `maxHeartRateBPM`、`activeCaloriesKcal`、`totalDistanceMeters`；`WorkoutObservationPrimitiveBuilder` 計算並傳遞這些值；`WorkoutObservation` 同步新增欄位；`secondarySignals` 顯示最高心率、消耗大卡、距離；`WeeklyObservationBuilder.totalActiveCalories` 從實際 workout 加總（不再回傳 nil）。
- **Phase D**：重排 `WorkoutDetailView`，新增 `HeroDecisionCardView`（tendency + goal-fit% + nextAction 為主視覺）、`KeyFindingsSectionView`（永遠可見）、`MetricsGridSectionView`（技術細節移入 DisclosureGroup）；zone chart、reasons、recommendations、advanced analysis 全部收納進「詳細數據」折疊區塊。
- swift test 130 tests 全通過，closeout 門禁全 PASS，commit 720ec35 推送至 origin/main。
